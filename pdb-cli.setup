;; -*- scheme -*-

(define info '((version "0.1")))

(define progs
  '("nodes-script"
    "import-script"
    "export-script"))

(for-each (lambda (prog)
            (compile -O3 -d1 ,(make-pathname #f prog "scm")))
          progs)

(install-program 'pdb-cli progs info)
