(compile -s -O3 -d1 nodes-script.scm)
(compile -s -O3 -d1 import-script.scm)
(compile -s -O3 -d1 export-script.scm)

(install-extension
  ;; Name of your extension:
  'pdb-cli
  ;; Files to install for your extension:
  '("nodes-script" "import-script" "export-script")
  ;; Assoc list with properties for your extension:
  '((version "0.1")))
